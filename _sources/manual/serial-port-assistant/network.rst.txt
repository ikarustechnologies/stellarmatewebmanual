.. section:: Network
    
Some devices can be connected to over the network (TCP/IP). For such devices, the only requirement is that the device is available on the same subnet as StellarMate. To connect to a networked device, you need its IP address (or hostname) and its port.